# MES Shopfloor Compose
This directory contains a compose script to run a dev-environment for the MES shopfloor development. You can either run the docker commands yourself and then cluster manually, or run either `. compose.ps1` or `compose.sh` depending on your OS. Both of these scripts get rid of the current installation of the compose stack _and clears configured volumes!_
